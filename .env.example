# Frontend public variables
# Local development: copy this file to .env.local and fill real values there.
# Production: configure these values in Cloudflare Pages environment variables when RAN-15 starts.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_WHATSAPP_NUMBER=5493582430953

# Backend/server secrets for future blocks only.
# Do not expose these in frontend code.
# Put real values only in Supabase secrets or provider dashboards, never in git.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Mailjet - future RAN-12
MAILJET_API_KEY=
MAILJET_SECRET_KEY=
MAILJET_FROM_EMAIL=
MAILJET_FROM_NAME=E-Motiva

# Google Drive - future RAN-13
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
GOOGLE_DRIVE_ROOT_FOLDER_ID=

# Admin notifications
ADMIN_NOTIFICATION_EMAIL=
